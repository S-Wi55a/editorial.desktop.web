@using Csn.Retail.Editorial.Web.Infrastructure.Extensions
@model Csn.Retail.Editorial.Web.Features.Details.Models.HeroSection

<img sizes="100vw"
     src="@Model.Images.First().GetHeroImageUrl(1024)"
     srcset="@Model.Images.First().GetHeroImageUrl(780) 1200w, @Model.Images.First().GetHeroImageUrl(642) 1024w @Model.Images.First().GetHeroImageUrl(492) 768w"
     alt="@Model.Images.First().AlternateText" />