@model Csn.Retail.Editorial.Web.Features.Details.Models.ArticleViewModel

@if (Model?.Price != null)
{
    <div class="article__pricing">
        @Model.Price.Currency@Model.Price.MinPrice - @Model.Price.Currency@Model.Price.MaxPrice
    </div>
}