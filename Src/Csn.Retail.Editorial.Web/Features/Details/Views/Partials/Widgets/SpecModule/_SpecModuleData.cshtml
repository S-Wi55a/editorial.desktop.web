@using Newtonsoft.Json
@model Csn.Retail.Editorial.Web.Features.Details.Models.ArticleViewModel


@if (Model.SpecData != null && Model.SpecData.Any())
{
    <script>
        var csn_editorial = csn_editorial || {};
        csn_editorial.specModule = @Html.Raw(JsonConvert.SerializeObject(Model.MoreArticleData)) || null;
    </script>
}
