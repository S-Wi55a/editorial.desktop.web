export const IS_PROD = process.env.NODE_ENV === 'production' ? true : false;
export const IS_DEV = process.env.NODE_ENV === 'development' ? true : false;
export const VIEW_BUNDLE = process.env.VIEW_BUNDLE === 'true' ? true : false;